/**
 * Public surface of @expense-tracker/database.
 *
 * `./generated/client` does not exist until `pnpm db:generate` has run — it is
 * gitignored build output. Unresolved-import errors here on a fresh clone are
 * expected; run `pnpm db:generate` and they go away.
 */
export * from "./generated/client";
